// Hand-written type aliases and extensions for the v3 SDK.
// Preserved across codegen runs via `output.clean: false` in openapi-ts.config.ts.

// Add custom v3 types here as needed — currently no consumers of v3.
export {}
